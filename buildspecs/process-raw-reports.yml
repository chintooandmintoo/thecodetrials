version: 0.2

phases:
  pre_build:
    commands:
      - echo "Preparing environment"
      - chmod +x process_raw_reports.sh

  build:
    commands:
      - ./process_raw_reports.sh

artifacts:
  files:
    - summary-report.json
  base-directory: .
